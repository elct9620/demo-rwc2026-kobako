<%# Replaced in place every time the writer produces another version, so this
    root element is what each broadcast aims at — and what makes the arrival
    visible, since every version lands as a fresh element. %>
<div id="<%= chat.script_id %>" class="script">
  <p class="state" data-state="<%= state %>"><%= state %></p>
  <pre><code><%= highlighted(chat.script) %></code></pre>
</div>
